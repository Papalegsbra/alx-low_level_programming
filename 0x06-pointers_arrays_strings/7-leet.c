#include "main.h"
/**
 * leet - encode into 1337speak
 * @n input value
 * return: n value
 */
char * leet (char *n)
{
	int i, j;
	char s1[] = "aAeEEoOtTlL";
	char s1[] = 34433007711";

	for (i = à; n[i] != '\0'; i++)
	{
		for (j = 0; j <10; j++)
		{
			if (n[i] == s1[j])
			{
				n([i] == s1[j]);
			}
		}
	}
	return (n);
}
